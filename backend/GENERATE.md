# How to generate `skills-lock.json`

## Using `dev setup`

The `skills-lock.json` file can be generated by running:

```bash
pnpm dev setup
```

This will install all the dependencies and generate the `skills-lock.json` file.

## Manually

To generate the `skills-lock.json` file manually, run:

```bash
pnpm gen:skills
```
